export const UI_REQUIREMENTS_BODY = `# UI 需求探索约束（ui / requirements）

本文件定义 sparrow-ui 阶段**必须遵守 / 禁止**的 UI 交互设计纪律。

> ⚠️ 本阶段为**可选阶段**。仅当项目需要前端界面时执行。

## 用户画像（Persona）

1. 根据 \`docs/sparrow/requirement/prd-business.md\` 中定义的**参与者（Actor）**，为每类参与者确定**用户画像**。
2. 用户画像必须包含：
   - 角色名称与职责
   - 核心目标与痛点
   - 技术熟练度
   - 使用场景与频率
3. **禁止**凭空捏造用户画像——必须从需求文档中的参与者推导。

## 用户旅程（User Journey）

1. 根据用户画像，为每类用户定义从**进入系统到完成目标**的完整旅程。
2. 用户旅程必须包含：
   - 旅程阶段（发现 → 探索 → 使用 → 完成 → 退出）
   - 每个阶段的用户操作与系统响应
   - 情感曲线（用户在各阶段的体验状态）
   - 关键触点与痛点
3. 用户旅程是 UI 页面设计的**输入**，不是 UI 页面本身。

## UI 页面定义

1. 根据用户旅程中的**关键触点**，定义参与交互和操作的 UI 页面。
2. 每个 UI 页面必须包含：
   - 页面名称与路径
   - 页面目的（解决哪个用户旅程阶段的哪个触点）
   - 页面结构（主要区域、组件层级）
   - 交互方式（用户可执行的操作及系统反馈）
   - 关联的限界上下文（该页面调用了哪个 BC 的服务）
3. **禁止**设计与用户旅程无关的页面。
4. **禁止**为尚不存在的业务服务设计 UI 页面。

## 原型页面纪律

1. 原型页面必须可独立打开和交互。
2. 原型页面使用语义化 HTML，**禁止**仅放置静态截图。
3. 原型中的交互流程必须与用户旅程一致。
4. 原型应体现设计令牌中的色彩和字体选择。
`;
